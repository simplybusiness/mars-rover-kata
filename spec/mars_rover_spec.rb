RSpec.describe "2 + 2" do
  describe '#initialize' do
    context 'with valid inputs' do
      it 'sets the x correctly'
      it 'sets the y correctly'
      it 'sets the direction correctly'
    end

    context 'with invalid inputs' do
      it 'raise an error when x is invalid'
      it 'raise an error when y is invalid'
      it 'raise an error when direction is invalid'
    end
  end
end
