---
name: test-driven-development
description: When asked to make a change that adds or removes functional code, it writes a failing test first, le't us run the test to watch it fail, allow us to make the failure informative, let us watch the test fail with more information, implement minimal code to make the test pass, look for smells in our code and refactor if necessary. Use when requests to add, remove or refactor code.
---